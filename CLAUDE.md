# 프로젝트 규칙

## 영상 시나리오 작성 규칙

### 1. 목소리는 반드시 서술한다 ★필수
**대사가 있는 모든 컷에 목소리 지정을 붙인다. 예외 없다.**
누가 말하는지만 쓰고 넘어가지 말고, **어떤 목소리로** 말하는지를 매번 적는다.
V.O.(내레이션), 인터뷰, 현장 대사, 전화 목소리 전부 해당.

표기: 대사 블록 바로 위에 `보이스:` 한 줄.
캐릭터별 기본 보이스 프로필은 `docs/voice-profiles.md`에 있고,
씬마다 달라지는 값(감정·속도·마이크 거리·처리)만 그 자리에 덧붙인다.

### 2. BGM은 넣지 않는다 ★필수
음악은 사용자가 직접 얹는다.
시나리오에는 **현장음·효과음·무음**만 설계한다.
"음악 최고조", "업템포 비트에 맞춰" 같은 음악 의존 연출 금지 —
같은 감정을 사운드로 대체해서 쓴다.

### 3. 이미지 생성
- 한 번에 **1장씩** 생성한다. 요청 없이 변형 여러 장 뽑지 않는다.
- 캐릭터 레퍼런스는 확정된 기준 이미지를 레퍼런스로 첨부해서 생성한다.

### 4. 생성 전 프롬프트 검사 ★필수
이미지·영상을 생성하기 전에 **프롬프트 전문을 사용자에게 먼저 보여주고 승인을 받는다.**
승인 없이 생성하지 않는다. 예외 없다.

### 5. 한글 대사는 이스케이프 없이 직접 입력 ★
생성 툴에 프롬프트를 넘길 때 한글을 `\uXXXX` 이스케이프로 쓰지 않는다.
한글 문자를 그대로 입력하고, 제출 후 에코된 프롬프트에서 대사를 눈으로 확인한다.
(이스케이프를 잘못 적어 대사가 틀린 채로 생성된 사고가 있었다)

### 6. 영상 비율 — 2화부터 9:16 세로 ★
배포처가 쓰레드·인스타·유튜브 숏츠다. 가로로 올리면 화면 절반이 빈다.

| | |
|---|---|
| 1화 | 16:9 (이미 생성됨). 세로 캔버스에 얹을 때 **리프레임하지 말고** 위아래를 제목 카드로 채운다 |
| **2화~** | **9:16 네이티브 생성** |

**세로에서는 투샷을 쓰지 않는다.** 두 사람을 나란히 놓으면 둘 다 작아져 표정이 안 읽힌다.
대화 씬은 **단독 샷을 교대로** 자른다. 자막은 하단에서 15% 이상 띄운다(플랫폼 UI가 가림).

### 7. 작가 톤 — 홍자매로 통일
《세 번째 만남》 전 화를 홍자매 톤으로 쓴다. 작가를 섞지 않는다.
감정 씬도 톤을 바꾸지 않고, **티키타카를 하다 한 사람이 안 받아치는 방식**으로 만든다.

## 산출물 위치

| 파일 | 내용 |
|---|---|
| `docs/wedding-video-scenario.md` | 식전영상 시나리오 《세 번째 만남》 |
| `docs/voice-profiles.md` | 캐릭터별 보이스 프로필 |
| `docs/character-reference-groom.md` | 신랑 캐릭터 레퍼런스 |
| `docs/character-reference-bride.md` | 신부 캐릭터 레퍼런스 |
| `docs/character-reference-support.md` | 조연(지원·준영) 캐릭터 레퍼런스 |
| `docs/location-reference-episode2.md` | 2화 장소 레퍼런스 (매장·방·식당 등) |
| `docs/episode1-hong.md` | 1화 컷 리스트 (홍자매 · **채택본**) |
| `docs/episode1-makjang.md` | 1화 컷 리스트 (임성한 · 보류) |
